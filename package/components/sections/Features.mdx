---
$type: Section
$id: https://mdx.org.ai/sections/Features
name: Features
description: Feature grid section
usedIn: [Home, Product]
---

# Features

A feature grid section displaying product features or benefits.

## Semantic Structure

```html
<section aria-label="Features">
  <header>
    <h2>Features</h2>
    <p>Everything you need to build amazing products</p>
  </header>
  <div>
    <article>
      <figure><!-- Icon --></figure>
      <h3>Feature Name</h3>
      <p>Feature description that explains the benefit.</p>
    </article>
    <article>
      <figure><!-- Icon --></figure>
      <h3>Feature Name</h3>
      <p>Feature description that explains the benefit.</p>
    </article>
    <!-- More features -->
  </div>
</section>
```

## CSS

```css
section[aria-label="Features"] {
  padding: var(--spacing-16) var(--spacing-4);
}

section[aria-label="Features"] > header {
  text-align: center;
  max-width: 600px;
  margin: 0 auto var(--spacing-12);
}

section[aria-label="Features"] > div {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: var(--spacing-8);
  max-width: 1200px;
  margin: 0 auto;
}

section[aria-label="Features"] article {
  text-align: center;
}

section[aria-label="Features"] article figure {
  width: 48px;
  height: 48px;
  margin: 0 auto var(--spacing-4);
  background: var(--primary);
  border-radius: var(--radius-lg);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--primary-foreground);
}

section[aria-label="Features"] article h3 {
  margin-bottom: var(--spacing-2);
}

section[aria-label="Features"] article p {
  color: var(--muted-foreground);
}
```

## Variants

### Left-aligned with icons
```css
section[aria-label="Features"] article {
  text-align: left;
  display: grid;
  grid-template-columns: 48px 1fr;
  gap: var(--spacing-4);
}
```

## Props

```typescript
interface FeaturesProps {
  title?: string
  description?: string
  items: Array<{
    icon: string | ReactNode
    title: string
    description: string
  }>
  columns?: 2 | 3 | 4
  variant?: 'centered' | 'left-aligned'
}
```
